/**
 * Browser polyfill for systeminformation
 * Returns empty/mock implementations since these are Node.js-only
 */

export default {};

// Export common types as any to prevent type errors
export type Systeminformation = any;

